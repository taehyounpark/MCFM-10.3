The purpose of the files in this directory is the NLO calculation of the process
	g + g -> h

in the HEFT (mt -> \infty limit).

The directory also includes files for the LO calculation of the same process
in the full Standard Model, accounting exactly for loops of top and bottom quarks.

